export type ProductCardProps = {
    imgSrc?: string;
    alt?: string;
    title?: string;
    description?: string;
    price?: number;
    //addToCart: ? add to cart function;
    className?: string };
